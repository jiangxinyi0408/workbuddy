# 纯手机方案 — 不用电脑也能完成

## ✅ 结论先行

**完全不需要电脑。** 你只需要一部手机，就能完成全部流程。

有两种纯手机方案，任选其一：

| 方案 | 优点 | 缺点 |
|------|------|------|
| **方案 A：我帮你直接部署**（推荐） | 最省事，我直接把代码推到你 GitHub | 需要你提供 GitHub Token |
| **方案 B：手机浏览器手动上传** | 不用提供 Token，全程自己操作 | 上传步骤稍繁琐 |

---

## 方案 A：我帮你直接部署到 GitHub（最省事）

你全程只需在手机上做 3 件事：

### 第 1 件事：注册 GitHub 账号（如已有跳过）

1. 手机浏览器打开 https://github.com/signup
2. 填邮箱、密码、用户名
3. 验证邮箱

### 第 2 件事：生成一个 Token 给我

1. 手机浏览器登录 GitHub，打开：
   https://github.com/settings/tokens/new
2. 填写：
   - **Note**：`workbuddy部署`（随便写，备注用）
   - **Expiration**：选 `30 days`（30天后过期，足够了）
   - **勾选权限**：只勾 ✅ `repo`（第一个大项，包含所有子项）
3. 拉到最底部，点击绿色按钮 **「Generate token」**
4. 页面会显示一串以 `ghp_` 开头的字符——**立即复制**它
   > ⚠️ 这串字符只显示一次，关掉就看不到了

### 第 3 件事：把 Token 和用户名告诉我

回到我们的对话，告诉我：
- 你的 **GitHub 用户名**
- 你刚复制的 **Token**（`ghp_` 开头那串）

我会帮你：
1. 在你 GitHub 创建 `workbuddy` 仓库
2. 把所有代码推上去
3. 开启 GitHub Pages
4. 把最终网址发给你

你拿到网址后，按下面的【第 4 步】添加到手机桌面即可。

> 🔒 Token 安全说明：我只会用它创建仓库和推送代码，不会做其他操作。用完后你可以到 GitHub Settings → Tokens 把它删除。

---

## 方案 B：手机浏览器手动上传（不提供 Token）

全程自己操作，分 4 步：

### 第 1 步：注册 GitHub 并建仓库

1. 手机浏览器打开 https://github.com/signup 注册
2. 登录后点右上角 **+** → **New repository**
3. 名称填 `workbuddy`
4. 选 **Public**
5. 勾选 **Add a README file**
6. 点 **Create repository**

### 第 2 步：把文件内容发到你自己邮箱

由于沙箱文件你手机直接看不到，最简单的办法是——**让我把所有代码打包，你通过邮箱接收**。

你可以这样做：
1. 在对话里告诉我你的邮箱地址
2. 我把所有文件内容整理好，你复制到手机备忘录
3. 然后逐个在 GitHub 网页创建文件

> 或者更简单：直接用方案 A，我帮你推上去。

### 第 3 步：在 GitHub 上传文件

在仓库页面：
1. 点 **Add file** → **Create new file**
2. 输入文件名（如 `index.html`）
3. 把代码内容粘贴进去
4. 点 **Commit changes**
5. 重复这个过程上传所有文件

需要创建的文件清单：
```
index.html
manifest.json
sw.js
css/style.css
js/app.js
js/db.js
js/ai.js
js/news.js
js/utils.js
js/modules/work.js
js/modules/weight.js
js/modules/finance.js
js/modules/english.js
js/modules/pingpong.js
```

### 第 4 步：开启 GitHub Pages

1. 仓库页面点 **Settings** → 左侧 **Pages**
2. Source 选 **main** 分支，文件夹选 **/ (root)**
3. 点 **Save**
4. 等 1-2 分钟，页面顶部出现网址：
   `https://你的用户名.github.io/workbuddy/`

---

## 第 4 步（通用）：手机添加为 App

无论方案 A 还是 B，拿到网址后这样做：

### 📱 iPhone（必须 Safari）

1. 打开 **Safari**（⚠️ 不是 Chrome）
2. 地址栏输入 `https://你的用户名.github.io/workbuddy/`
3. 等页面加载完
4. 点底部 **分享按钮**（方块+向上箭头 ↗）
5. 找到 **「添加到主屏幕」** → 点
6. 点右上角 **「添加」**
7. 主屏幕出现工作台图标 🎉

### 📱 Android（Chrome）

1. 打开 **Chrome**
2. 输入网址
3. 点右上角 **⋮** → **「添加到主屏幕」** 或 **「安装应用」**
4. 点 **「添加」** 🎉

---

## 首次打开配置

1. 点左侧导航底部的 **⚙️ 设置** → **应用设置**
2. 填身高、目标体重
3. 配置 AI 食物识别（可选）：
   - 浏览器打开 https://aistudio.google.com/apikey
   - 登录 Google → Create API Key → 复制
   - 粘贴到设置页的 Gemini API Key 框
4. 点 **保存设置**

健康指标已预填：**低密度脂蛋白过高**、**胆固醇过高**，不用改。

---

## 总结

| 你的情况 | 推荐方案 | 你要做的 |
|----------|----------|----------|
| 想最省事 | 方案 A | 注册GitHub → 生成Token → 告诉我用户名和Token |
| 不想给Token | 方案 B | 注册GitHub → 手机网页逐个创建文件 |

**两种方案都不需要电脑，纯手机就能完成。**

如果选方案 A，现在就去注册 GitHub、生成 Token，然后把用户名和 Token 发给我，剩下的我来搞定。
