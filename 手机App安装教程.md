# 把工作台变成手机 App — 手把手教程

## 整体流程

```
应用文件（沙箱）
    ↓ 第1步：下载文件到你电脑
    ↓ 第2步：上传到 GitHub
    ↓ 第3步：开启 GitHub Pages（免费获得网址）
    ↓ 第4步：手机浏览器打开网址
    ↓ 第5步：添加到主屏幕 → 变成 App
```

---

## 第 1 步：从沙箱下载应用文件

在沙箱的文件界面中，把 `/workspace` 目录下的**所有文件和文件夹**下载到你电脑上：

需要下载的内容：
- `index.html`
- `manifest.json`
- `sw.js`
- `css/` 文件夹（含 style.css）
- `js/` 文件夹（含 app.js, db.js, ai.js, news.js, utils.js 和 modules/ 子文件夹）

> ⚠️ **重要**：保持目录结构不变！`css/style.css` 和 `js/modules/*.js` 的路径不能变。

下载后在电脑上建一个文件夹（比如叫 `workbuddy`），把所有文件放进去。

---

## 第 2 步：注册 GitHub 账号（如果没有）

1. 电脑浏览器打开 https://github.com/signup
2. 填写邮箱、密码、用户名
3. 验证邮箱
4. 免费账号即可，不需要付费

> 如果你已有 GitHub 账号，跳过这步。

---

## 第 3 步：在 GitHub 创建仓库并上传文件

### 3.1 创建新仓库
1. 登录 GitHub，点击右上角 **+** → **New repository**
2. Repository name 填：`workbuddy`
3. 选择 **Public**（公开，GitHub Pages 免费版需要公开）
4. 勾选 **Add a README file**
5. 点击 **Create repository**

### 3.2 上传文件
1. 在仓库页面点击 **「Add file」** → **「Upload files」**
2. 把你电脑 `workbuddy` 文件夹里的文件**拖拽**到上传区域
   - 注意：`css` 和 `js` 文件夹需要单独处理
3. 上传 `index.html`、`manifest.json`、`sw.js`、`README.md` 直接拖入
4. **创建 css 文件夹**：
   - 点击 **「Add file」** → **「Create new file」**
   - 输入 `css/style.css`
   - 把你电脑上 `css/style.css` 的内容复制粘贴进去
   - 点击 **Commit changes**
5. **创建 js 文件夹下的文件**，同样方式逐个创建：
   - `js/app.js`
   - `js/db.js`
   - `js/ai.js`
   - `js/news.js`
   - `js/utils.js`
   - `js/modules/work.js`
   - `js/modules/weight.js`
   - `js/modules/finance.js`
   - `js/modules/english.js`
   - `js/modules/pingpong.js`

> 💡 每个文件都复制内容粘贴，然后 Commit。虽然繁琐，但只需做一次。

---

## 第 4 步：开启 GitHub Pages（获得网址）

1. 在你的仓库页面，点击顶部 **「Settings」** 标签
2. 左侧菜单找到 **「Pages」**
3. 在 **Source** 下拉框选择 **「main」** 分支
4. 文件夹选 **「/ (root)」**
5. 点击 **「Save」**
6. 等待 1-2 分钟，刷新页面
7. 页面顶部会显示：**"Your site is published at https://你的用户名.github.io/workbuddy/"**

这就是你的 App 网址！把它记下来。

---

## 第 5 步：在手机上添加为 App

### 📱 iPhone（必须用 Safari）

1. 打开 **Safari 浏览器**（⚠️ 不是 Chrome）
2. 地址栏输入你的网址：
   ```
   https://你的用户名.github.io/workbuddy/
   ```
3. 等待页面完全加载
4. 点击 Safari 底部的 **分享按钮**（一个方块带向上箭头 ↗）
5. 在弹出的菜单中向下滑动
6. 找到并点击 **「添加到主屏幕」**
7. 可以修改名称（默认"工作台"），点击右上角 **「添加」**
8. 返回手机主屏幕，你会看到工作台图标 🎉
9. 点击图标，全屏打开使用！

### 📱 Android（用 Chrome）

1. 打开 **Chrome 浏览器**
2. 地址栏输入你的网址
3. 等待页面加载
4. 点击右上角 **三个点 ⋮**
5. 点击 **「添加到主屏幕」** 或 **「安装应用」**
6. 点击 **「添加」** 或 **「安装」**
7. 返回主屏幕，找到工作台图标 🎉
8. 点击图标打开使用！

---

## 第 6 步：首次使用配置

打开 App 后，按以下顺序设置：

### 6.1 打开设置
- 点击左侧导航栏底部的 **「⚙️ 设置」**
- 点击 **「应用设置」**

### 6.2 填写基本信息
- **身高**：如 `175`（用于 BMI）
- **目标体重**：如 `70`（你的减肥目标）
- **每日工作总结时间**：默认 21:00，可调整

### 6.3 配置 AI 食物识别（推荐）

1. 电脑浏览器打开：https://aistudio.google.com/apikey
2. 用 Google 账号登录
3. 点击 **「Create API Key」**
4. 复制生成的 Key（格式 `AIzaSy...` 开头）
5. 回到工作台 App 的设置页
6. 粘贴到 **「Gemini API Key」** 输入框
7. 点击 **「保存设置」**

> ✅ 配置后，身材管理模块可以拍照自动识别食物卡路里
> ❌ 不配置也能用，手动输入食物即可

### 6.4 健康指标（已预填）
- 已为你设置：**低密度脂蛋白过高**、**胆固醇过高**
- 饮食推荐会基于这两个指标生成低胆固醇食谱
- 不需要额外操作

---

## 完成！🎉

现在你已经拥有一个专属的个人工作台 App 了：

- 📋 **每日业务安排**：记录任务、每日自动总结、智能工时推算
- 🏓 **打球时间**：登记乒乓球活动、自动计算自由时间
- 📚 **英语能力提升**：90天口语计划、每日任务推送
- ⚖️ **身材管理**：体重曲线、饮食拍照识别、低胆固醇食谱推荐
- 📰 **每日资讯**：国内外热点新闻
- 💰 **还款进度**：贷款管理、收入记录、还款追踪

---

## 🔧 后续更新

如果需要修改功能或修复问题：
1. 告诉我需要改什么
2. 我在沙箱里修改文件
3. 你重新下载修改的文件，上传到 GitHub 覆盖
4. 1-2 分钟后 GitHub Pages 自动更新
5. 手机上刷新即可看到新版本

---

## ❓ 常见问题

**Q: GitHub Pages 网址打不开？**
A: 等待 2-3 分钟让部署完成，检查文件路径是否正确（特别是 js/modules/ 下的文件）。

**Q: iPhone 添加到主屏幕后没有图标？**
A: 必须用 Safari 打开，Chrome 不支持"添加到主屏幕"。

**Q: 离线能用吗？**
A: 首次联网加载后，后续可以离线使用（数据存在手机本地）。AI 识别和新闻需要联网。

**Q: 数据会丢失吗？**
A: 不会。数据存在手机 IndexedDB 中。但换手机需先在设置里"导出全部数据"备份。

**Q: AI 识别收费吗？**
A: Gemini API 免费额度充足，个人使用完全免费。
